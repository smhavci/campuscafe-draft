[project]
name = "campuscafe-crew"
version = "0.1.0"
description = "CampusCafe AI Agent powered by CrewAI"
requires-python = ">=3.10,<=3.13"
dependencies = [
    "crewai[tools]>=0.108.0",
    "python-dotenv>=1.0.0",
    "requests>=2.31.0",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends._legacy:_Backend"
